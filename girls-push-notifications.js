/* Girls Web Push. App preference defaults ON; browser permission is still user-controlled. */
(()=>{
'use strict';
if(window.__GTG_PUSH_NOTIFICATIONS__)return;window.__GTG_PUSH_NOTIFICATIONS__=true;
const VAPID_PUBLIC='BBOvE39Xoa64X0oWNOAkUHAdaXvVxJ659yxEUipBEiLVisNIQqabKrnMoptXxGWnr9SG8-QMp8b0r-hHrtixoxg';
const SUPABASE_URL='https://vtcmvwixfqyxqghibsla.supabase.co';
const PUBLISHABLE_KEY='sb_publishable_qBQzJjFxSToEGxPJEcmskg_GNd4M4cP';
const ENDPOINT=`${SUPABASE_URL}/functions/v1/push-register`,PRODUCT='girls',PREF='gtg-push-optin-v1';
let client=null;
if(localStorage.getItem(PREF)===null)localStorage.setItem(PREF,'1');
const wanted=()=>localStorage.getItem(PREF)!=='0';
const supported=()=>('Notification'in window)&&('serviceWorker'in navigator)&&('PushManager'in window);
const keyBytes=value=>{const padding='='.repeat((4-value.length%4)%4),base64=(value+padding).replace(/-/g,'+').replace(/_/g,'/'),raw=atob(base64),out=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)out[i]=raw.charCodeAt(i);return out};
function db(){if(client)return client;if(!window.supabase?.createClient)return null;client=window.supabase.createClient(SUPABASE_URL,PUBLISHABLE_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});return client}
async function session(){const c=db();if(!c)return null;try{return (await c.auth.getSession()).data?.session||null}catch{return null}}
async function post(action,subscription){const s=await session();if(!s?.access_token)throw new Error('Sign in required.');const data=subscription.toJSON(),keys=data.keys||{};const response=await fetch(ENDPOINT,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${s.access_token}`},body:JSON.stringify({action,product_key:PRODUCT,platform:'web',provider:'webpush',endpoint:subscription.endpoint,p256dh:keys.p256dh||null,auth:keys.auth||null})});const result=await response.json().catch(()=>({}));if(!response.ok)throw new Error(result.error||'Notification registration failed.');return result}
async function current(){if(!supported())return null;const registration=await navigator.serviceWorker.ready;return registration.pushManager.getSubscription()}
async function activateGranted(){if(!supported()||Notification.permission!=='granted')return null;const registration=await navigator.serviceWorker.ready;let subscription=await registration.pushManager.getSubscription();if(!subscription)subscription=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:keyBytes(VAPID_PUBLIC)});await post('register',subscription);localStorage.setItem(PREF,'1');return subscription}
async function enable(){if(!supported())throw new Error('Push notifications are not supported on this device.');localStorage.setItem(PREF,'1');const permission=Notification.permission==='granted'?'granted':await Notification.requestPermission();if(permission!=='granted')throw new Error(permission==='denied'?'Notifications are blocked in browser settings.':'Notifications were not enabled.');return activateGranted()}
async function disable(){const subscription=await current();if(subscription){try{await post('unregister',subscription)}catch(error){console.warn('[GTG push] unregister failed',error)}await subscription.unsubscribe().catch(()=>{})}localStorage.setItem(PREF,'0')}
function statusText(subscription){if(!supported())return'Not supported on this device';if(!wanted())return'Off';if(Notification.permission==='denied')return'On in app · blocked in browser settings';if(subscription&&Notification.permission==='granted')return'On for this device';return'On by default · allow browser permission'}
async function refresh(root){const status=root?.querySelector('[data-gtg-push-status]'),button=root?.querySelector('[data-gtg-push-toggle]');if(!status||!button)return;const subscription=await current().catch(()=>null),active=Boolean(wanted()&&subscription&&Notification.permission==='granted');status.textContent=statusText(subscription);button.textContent=active?'Turn off notifications':wanted()?'Enable on this device':'Turn on notifications';button.disabled=!supported()||Notification.permission==='denied';button.dataset.pushOn=active?'1':'0'}
function decorateProfile(){const form=document.querySelector('#profileForm');if(!form||form.querySelector('[data-gtg-push-settings]'))return false;const actions=form.querySelector('.form-actions, .actions, [data-profile-actions]'),section=document.createElement('div');section.className='field';section.dataset.gtgPushSettings='1';section.innerHTML='<label>Push notifications</label><p class="field-note">Notifications are on by default. Your browser still controls permission for this device.</p><button class="ghost" type="button" data-gtg-push-toggle>Checking…</button><small class="field-note" data-gtg-push-status>Checking…</small>';if(actions)form.insertBefore(section,actions);else form.append(section);void refresh(section);return true}
async function silentRefresh(){if(!supported()||!wanted()||Notification.permission!=='granted')return;try{await activateGranted()}catch(error){console.warn('[GTG push] refresh failed',error)}}
function scheduleProfileCheck(){[0,80,240,600].forEach(ms=>setTimeout(()=>decorateProfile(),ms))}
document.addEventListener('click',event=>{const toggle=event.target.closest?.('[data-gtg-push-toggle]');if(toggle){event.preventDefault();const root=toggle.closest('[data-gtg-push-settings]');toggle.disabled=true;const run=toggle.dataset.pushOn==='1'?disable():enable();void run.catch(error=>{const toast=document.getElementById('toast');if(toast){toast.textContent=error?.message||'Notifications could not be changed.';toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3200)}}).finally(()=>void refresh(root));return}scheduleProfileCheck()},true);
window.addEventListener('pageshow',()=>{scheduleProfileCheck();void silentRefresh()},{once:true});
scheduleProfileCheck();
})();
